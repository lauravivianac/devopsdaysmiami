<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- fleetview-codex-bridge:start -->
## Read CLAUDE.md first

This repo's real operating instructions — the Team Room posting protocol, the active team roster, focus folder scope, and every other FleetView convention — live in `CLAUDE.md` at the repo root, not here. Read it in full before starting any task and follow it exactly, the same way Claude Code does automatically. This file exists only because Codex reads AGENTS.md, not CLAUDE.md, by its own convention — everything that actually matters is over there, not duplicated here where it could drift out of sync.

Full protocol: `CLAUDE.md`. Read it first.
<!-- fleetview-codex-bridge:end -->
